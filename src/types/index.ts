export type Theme = {
  primary: string;
  secondary: string;
  bg: string;
  accent1: string;
  accent2: string;
  default: string;
};

export type DropFieldBgs = {
  primary: string;
  secondary: string;
  bg: string;
  accent1: string;
  accent2: string;
  default: string;
};
