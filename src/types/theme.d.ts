declare type Theme = {
  primary: string;
  secondary: string;
  bg: string;
  accent1: string;
  accent2: string;
  // supposed to be a hardcoded color, to fallback to the default in 'onClickRemoveColor'
  default: string;
};
